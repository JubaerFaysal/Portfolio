class NavLink {
  final String
      href; // In Flutter, this might be a key or an index for scrolling
  final String label;

  const NavLink({
    required this.href,
    required this.label,
  });
}
